const FirstComponent = () => {
    const name = "Gabriel";

    return (
        <div>
            <h1>First component of {name}</h1>
            <div>

            </div>
        </div>
    );
};

export default FirstComponent;